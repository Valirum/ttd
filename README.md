# TTD - Task Manager

## Installation
cargo install --path .

## Usage
See `td --help`

## Storage
~/.config/ttd/tasks.json 

## Config
~/.config/ttd/config.toml 

Sessions are named maps of tasks.
Task has description, optional time, done flag.
Fuzzy matching for task selection by description.
